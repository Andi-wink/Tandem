# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

/experiments

# Local Hugging Face weight cache (self-hosted Voxtral eval) — never commit.
/hf_cache

# dependencies
/node_modules
**/models
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions
*.mp4

# testing
/coverage
/dist

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# Audio files
*.wav

# Large media files
docs/demo.mov
docs/demo.gif


meeting_minutes.db

# Added by Task Master AI
# Logs
logs
*.log
dev-debug.log
# Dependency directories
node_modules/
# Environment variables
.env
# Editor directories and files
.idea
.vscode
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
experiments/
.cursor/
target/

frontend/src-tauri/binaries/
# Ignore Cargo.lock everywhere (vendored/sub-crate locks) EXCEPT the workspace
# root lock: this is a Tauri application, so its lock is committed to guarantee
# reproducible builds across worktrees/CI (an unpinned tauri caret range drifts
# to versions whose macros break generate_handler!).
Cargo.lock
!/Cargo.lock

# Python
.venv/
__pycache__/
*.pyc

# Claude Code
.claude/

# Dogfooding — local meeting recordings
meetings/

# Client-derived conversation data — NEVER commit. This repo is public.
# .tandem/ holds verbatim call transcripts, the session feed, task handoffs
# and screenshots captured during live calls. See research/gdpr-review-*.md (F1).
.tandem/
Tandem meeting notes/
*/live-transcript.md

# Privacy review maps where every plaintext credential on the machine lives.
research/gdpr-review-*.md

# Candid assessment of our own compliance gaps. Neutral market/engine research
# (european-notetaker-services-*, german-asr-*) stays tracked and is safe to publish.
research/german-calls-*.md
